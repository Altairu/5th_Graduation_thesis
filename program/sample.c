#include <stdio.h>

int main()
{
    int a = 10;                    // aに10を代入
    int b = 20;                    // bに20を代入
    printf("a + b = %d\n", a + b); // a + bの結果を出力
    return 0;                      // プログラム正常終了
}